#!/bin/bash

cat <<'MOTD'
*********************************************************************

  ╔╦╗┬┌┐┌┬ ┬    ╦┌─┐┌┐ ┌─┐
   ║ │││││ │    ║│ │├┴┐└─┐
   ╩ ┴┘└┘ ┴   ╚╝└─┘└─┘└─┘

  Slack-native AI Agent Platform powered by Claude

*********************************************************************
MOTD

if [ -f /opt/tinyjobs/.needs-setup ]; then
  cat <<'SETUP'

  To complete setup, run:

    /opt/tinyjobs-setup.sh

  This will guide you through:
    1. Creating your Slack app
    2. Entering your API keys
    3. Starting TinyJobs

SETUP
else
  cat <<'RUNNING'

  TinyJobs is running.

  Useful commands:
    Status:   cd /opt/tinyjobs && docker compose ps
    Logs:     cd /opt/tinyjobs && docker compose logs -f tinyjobs
    Restart:  cd /opt/tinyjobs && docker compose restart
    Update:   cd /opt/tinyjobs && git pull && docker compose up -d --build

RUNNING
fi

echo "  Docs: https://github.com/anantgarg/tinyjobs"
echo ""
